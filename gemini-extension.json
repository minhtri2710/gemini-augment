{
	"name": "gemini-augment",
	"version": "0.1.0",
	"description": "Augment-style prompt rewriting for Gemini CLI via /augment.",
	"contextFileName": "GEMINI.md",
	"mcpServers": {
		"gemini-augment": {
			"command": "node",
			"args": ["${extensionPath}${/}dist${/}src${/}index.js"],
			"cwd": "${extensionPath}"
		}
	}
}
